import React from "react";

// Context allows me to pass data to components without passing down props at every level
const DataAreaContext = React.createContext({});

export default DataAreaContext;
